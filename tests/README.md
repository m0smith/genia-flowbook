# Tests

Suggested structure:

- `tests/unit/` for small focused tests
- `tests/integration/` for end-to-end behavior
- `tests/data/` for example sidecar files and test fixtures

Every documented public behavior should eventually have matching tests.
